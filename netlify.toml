[build]
  command = "npm ci && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

# Geo-redirect: Italian visitors → /it/
[[redirects]]
  from = "/"
  to = "/it/"
  status = 302
  force = false
  conditions = {Country = ["IT"]}
